# 硬件参考

## SoC: MSPM0G3507

| 参数 | 值 |
|------|------|
| Core | ARM Cortex-M0+ (无 FPU) |
| 主频 | 80 MHz |
| Flash | 128 KB (带 ECC) |
| SRAM | 32 KB |
| SPI 最高速率 | 32 Mbps |
| I²C 最高速率 | 1 Mbps (Fast-mode Plus) |
| DMA | 7 通道 |
| 数学加速 | DIV, SQRT, MAC, TRIG (硬件) |

## 显示

| 参数 | 值 |
|------|------|
| 型号 | TELESKY MK000740 |
| 类型 | 高清 IPS 彩色 TFT |
| 驱动 IC | ST7789 |
| 接口 | 4 线 SPI |
| 物理分辨率 | 240 × 240 |
| 像素格式 | RGB565 (2 字节/像素) |
| 全分辨率帧缓冲 | 240 × 240 × 2 = 115,200 字节 (115 KB) |

### 帧缓冲策略

32 KB SRAM 装不下 115 KB 的全分辨率帧缓冲，因此帧缓冲设为 **80 × 80（12.5 KB，~40% SRAM）**。

ST7789 **不支持硬件像素缩放**。帧缓冲需在 MCU 端软件放大 3 倍为 240×240 后通过 SPI 发送。

全分辨率帧缓冲太大，无法在 MCU 端完整展开后再发送。实际方案为**逐行缩放 + DMA 传输**：
- 从 80×80 帧缓冲读取 1 行 → 水平 3x 扩展 → 得到 240 像素行缓冲 → DMA 推送到 SPI → 同一行重复发送 3 次实现垂直 3x 扩展
- 行缓冲: 240 像素 × 2 字节 = 480 字节（可忽略不计）

## 帧率

缩放路径下每帧 SPI 传输量: 240 行 × (240 像素 × 2 字节) = 115,200 字节 = 115 KB

| SPI 速率 | 115 KB 传输耗时 |
|----------|----------------|
| 20 MHz | ~46 ms |
| 24 MHz | ~38 ms |
| 32 MHz (MCU 上限) | ~29 ms |

**目标: 15 fps (66.7 ms/帧)**，使用 24 MHz SPI

每帧预算分解：

| 阶段 | 估算耗时 |
|------|----------|
| 清空帧缓冲 (80×80, 6400 像素写) | ~0.3 ms |
| 输入采样 + 游戏逻辑 | ~1 ms |
| 渲染到 80×80 帧缓冲 | ~2-5 ms |
| CPU 3x 逐行缩放 | ~3 ms |
| SPI 传输 (24 MHz) | ~38 ms |
| **合计** | **~45-48 ms** |

余量约 18 ms，为场景复杂度波动留足空间。

15fps 对 80×80 掌机屏完全可接受——多数同类设备也在 10-20fps 区间。
若未来场景复杂度增长或需要更高帧率，可考虑 32 MHz SPI → 可实现约 20fps。

不使用 30fps 的原因：
- 30fps 需 32 MHz SPI 极限速率，传输就吃掉 29ms，仅余 4ms 给游戏逻辑和渲染
- 任何复杂度波动都会丢帧，实际体验反而不如稳定的 15fps

## 音频

| 参数 | 值 |
|------|------|
| 发声硬件 | 单路蜂鸣器 (PWM 驱动) |
| 音量控制 | 无 (仅开关) |
| 音调序列 | 需要定时器中断驱动 |

## 输入

按键掩码: 8 个按钮 (UP/DOWN/LEFT/RIGHT/A/B/START/SELECT)，位掩码存储在 uint8_t 中。
