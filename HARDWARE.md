# 硬件参考

## SoC: MSPM0G3507

| 参数 | 值 |
|------|------|
| Core | ARM Cortex-M0+ (无 FPU) |
| 主频 | 80 MHz |
| Flash | 128 KB (带 ECC) |
| SRAM | 32 KB |
| 数学加速 | DIV, SQRT, MAC, TRIG (硬件) |

## 显示

| 参数 | 值 |
|------|------|
| 分辨率 | 80 × 80 |
| 像素格式 | RGB565 (2 字节/像素) |
| 帧缓冲大小 | 80 × 80 × 2 = 12,800 字节 (12.5 KB) |
| 帧缓冲占 SRAM | ~40% |

## 帧率

**目标: 30 fps (33.3 ms/帧)**

30fps 下每帧预算分解 (估算)：

| 阶段 | 估算耗时 |
|------|----------|
| 清空帧缓冲 (6400 像素写) | ~0.3 ms |
| 输入采样 + 场景逻辑 | ~1 ms |
| 绘制调用 | ~2-5 ms |
| SPI 传输 (10-20 MHz) | ~6-10 ms |
| **合计** | **~10-16 ms** |

余量约 17 ms，为渲染复杂度和 SPI 延迟留足空间。

60fps (16.6 ms/帧) 不设为目标，原因：
- 80 MHz Cortex-M0+ 上帧缓冲清空 + 绘制 + SPI 传输已接近预算上限
- 余量不足，复杂度波动易导致丢帧
- 30fps 电池寿命更长

## 音频

| 参数 | 值 |
|------|------|
| 发声硬件 | 单路蜂鸣器 (PWM 驱动) |
| 音量控制 | 无 (仅开关) |
| 音调序列 | 需要定时器中断驱动 |

## 输入

按键掩码: 8 个按钮 (UP/DOWN/LEFT/RIGHT/A/B/START/SELECT)，位掩码存储在 uint8_t 中。
