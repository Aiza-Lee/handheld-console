# Game Core

This directory contains the portable runtime, platform interfaces, null platform, and game logic shared by the firmware target and the host-side simulator.

## Layout

- `include/`: public headers and header-only helpers
- `src/`: portable source files
- `tests/`: host-side smoke tests without TI/board dependencies
